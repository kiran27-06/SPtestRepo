# Hashassin
Hashassin is a Rust-based tool for generating and cracking password hashes.

## Structure
- `cli/` - Command-line interface for interacting with Hashassin.
- `core/` - Library that implements core hashing functionalities.

## Usage
To build and run the project:
```bash
cargo run --manifest-path cli/Cargo.toml
